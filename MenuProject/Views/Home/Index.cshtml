<div class="hero-container">
    <h1 class="hero-title">Sayfama Hoşgeldiniz.</h1>
    <p class="hero-subtitle">Sizlere en iyi deneyimi sunmak için buradayız.</p>
</div>

<style>
    /* Gökkuşağı animasyonlu arka plan */
    body {
        background: linear-gradient(270deg, #ff0000, #ff9900, #ffff00, #33cc33, #0099ff, #6600cc);
        background-size: 400% 400%;
        animation: rainbowBG 8s ease infinite;
        color: white;
        font-family: 'Arial', sans-serif;
        text-align: center;
        height: 100vh;
        margin: 0;
        display: flex;
        justify-content: center;
        align-items: center;
    }

    /* Arka planın sürekli renk değiştirmesi için animasyon */
    @@keyframes rainbowBG {
        0% {
            background-position: 0% 50%;
        }

        50% {
            background-position: 100% 50%;
        }

        100% {
            background-position: 0% 50%;
        }
    }

    /* Hero bölümünü ortala */
    .hero-container {
        padding: 20px;
        border-radius: 10px;
        background: rgba(0, 0, 0, 0.2); /* Saydam kutu */
        box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.3);
    }

    /* Başlık tasarımı */
    .hero-title {
        font-size: 3rem;
        font-weight: bold;
        text-shadow: 2px 2px 5px rgba(0, 0, 0, 0.3);
        animation: fadeIn 1.5s ease-in-out;
    }

    /* Alt başlık tasarımı */
    .hero-subtitle {
        font-size: 1.5rem;
        margin-top: 10px;
        opacity: 0.9;
        animation: fadeIn 2s ease-in-out;
    }

    /* Fade-in animasyonu */
    @@keyframes fadeIn {
        from {
            opacity: 0;
            transform: translateY(-20px);
        }

        to {
            opacity: 1;
            transform: translateY(0);
        }
    }
</style>